{
  "name": "the-she-archive",
  "version": "1.0.0",
  "description": "The She Archive - Static site with CMS",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/generate-posts-index.js && npx tailwindcss -i src/input.css -o public/styles/tailwind.css --minify",
    "start": "npx serve public"
  },
  "dependencies": {
    "marked": "^4.3.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.11",
    "front-matter": "^4.0.2",
    "tailwindcss": "^3.4.19"
  }
}
